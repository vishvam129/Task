from string_utils import operations as o

line=input("Enter a String u want to reverse :- ")
print(o.reversed_string(line))

line=input("Enter a String in which u want to count the vowels :- ")
print(o.count_vowels(line))

line=input("Enter a String which u want to check palindrome :- ")
print(o.is_palindromes(line))